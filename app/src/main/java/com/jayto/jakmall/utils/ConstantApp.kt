package com.jayto.jakmall.utils

object ConstantApp {

    interface PREF {
        companion object {
            const val AUTH_TOKEN = "AUTH_TOKEN"
            const val AUTH_USER = "AUTH_USER"
        }
    }

    interface PATH_ENDPOINT {
        companion object {
            const val CATEGORIES = "categories"
        }
    }
}